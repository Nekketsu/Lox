class Circle {
    init(radius) {
        this.radius = radius;
    }

    area() {
        return 3.14159263 * this.radius * this.radius;
    }
}

var circle = Circle(4);
print circle.area(); // Prints roughly "50.2655".
